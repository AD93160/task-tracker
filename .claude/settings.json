{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "$CLAUDE_PROJECT_DIR/.claude/hooks/session-start.sh"
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | { read -r f; [ -n \"$f\" ] && git add \"$f\" && (git diff --cached --quiet || (git commit -m \"auto: update $(basename \"$f\")\" && git push -u origin main)); } 2>/dev/null || true",
            "statusMessage": "Commit & push…"
          }
        ]
      }
    ]
  }
}
